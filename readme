1. Instalar **Java 17**, **Maven** y **PostgreSQL**.  

2. Crear la base de datos en PostgreSQL:  
Ejecutar el script schema.sql para crear las tablas auth_users y usuarios con datos iniciales.
Configurar el archivo src/main/resources/application.properties con tu usuario y contraseña de PostgreSQL

3. En la raíz del proyecto, ejecutar:

bash
mvn spring-boot:run
4. Abrir en el navegador:

http://localhost:3000
Ingresar con las credenciales de demo:

Usuario: admin

Contraseña: utec123
